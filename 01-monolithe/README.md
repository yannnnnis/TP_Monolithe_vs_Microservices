# Application monolithique

Une seule application déployable regroupe commandes, catalogue et paiement.

Lancer :
```bash
docker compose up --build
```

API : http://localhost:8000

Le pipeline `.gitlab-ci.yml` illustre un pipeline centralisé.
